% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/StabilityPackage.R
\name{stability}
\alias{stability}
\title{Stability estimation with resampling}
\usage{
stability(
  data,
  response,
  predictors,
  B = 50,
  resample = c("bootstrap", "subsample"),
  m = NULL,
  delta = 2,
  eps = 0.5,
  L = 20,
  K = length(predictors),
  verbose = TRUE
)
}
\arguments{
\item{data}{Data frame containing the response and predictors.}

\item{response}{Name of the response variable (character).}

\item{predictors}{Character vector of predictor column names in \code{data}.}

\item{B}{Number of resamples (bootstrap samples or subsamples).}

\item{resample}{Resampling scheme: \code{"bootstrap"} or \code{"subsample"}.}

\item{m}{Subsample size when \code{resample = "subsample"}; defaults to
\code{floor(0.8 * nrow(data))} if \code{NULL}.}

\item{delta}{Tuning parameter passed to \code{build_paths()} controlling the
allowable AIC distance from the best child.}

\item{eps}{Tuning parameter passed to \code{build_paths()} for the minimum
AIC improvement over the parent.}

\item{L}{Maximum number of models kept at each step in \code{build_paths()}.}

\item{K}{Maximum number of forward steps (defaults to \code{length(predictors)}).}

\item{verbose}{Logical; if \code{TRUE}, prints progress over resamples.}
}
\value{
A named numeric vector of length \code{length(predictors)}, with values in
\code{[0, 1]} giving the estimated stability of each predictor.
}
\description{
Estimates selection stability by running \code{build_paths()} on resampled
data and recording how often each predictor appears in the models.
}
\examples{
\dontrun{
set.seed(1)
dat <- data.frame(
  y  = rnorm(50),
  x1 = rnorm(50),
  x2 = rnorm(50),
  x3 = rnorm(50)
)

stability(
  data       = dat,
  response   = "y",
  predictors = c("x1", "x2", "x3"),
  B          = 10,
  resample   = "bootstrap"
)
}
}
