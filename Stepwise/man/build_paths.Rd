% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forward_path_selection.R
\name{build_paths}
\alias{build_paths}
\title{Build forward selection paths}
\usage{
build_paths(
  data,
  response,
  predictors,
  delta = 2,
  eps = 0.5,
  L = 20,
  K = length(predictors),
  verbose = TRUE,
  debug = FALSE
)
}
\arguments{
\item{data}{A data frame containing the response and predictors}

\item{response}{Name of the response variable (string)}

\item{predictors}{Vector of predictor variable names (strings)}

\item{delta}{Numeric. Allowable AIC distance from the parent’s best child (default 2)}

\item{eps}{Numeric. Minimum required AIC improvement over parent (default 0.5)}

\item{L}{Integer. Maximum number of models kept per step (default 20)}

\item{K}{Integer. Maximum number of steps / model size (default = length(predictors))}

\item{verbose}{Logical. Whether to print progress (default TRUE)}

\item{debug}{Logical. Whether to print per-step debug info (default FALSE)}
}
\value{
A list with:
  - path_forest: list of data frames with models and AIC per step
  - aic_by_model: named vector of AIC for each model
  - meta: list with parameters and info
}
\description{
Performs forward-path selection starting from the empty model, adding one variable at a time,
keeping children within delta of the best AIC and exceeding minimum improvement eps.
If no children meet eps, the best child is always kept to allow multistep selection.
Deduplicates and prunes to a maximum L models per step.
}
\examples{
set.seed(1)
n <- 120; p <- 8
X <- matrix(rnorm(n*p), n, p)
colnames(X) <- paste0("x", 1:p)
beta <- c(2, -1.5, 0, 0, 1, rep(0, p-5))
y <- as.numeric(X \%*\% beta + rnorm(n))  # numeric vector
dat <- data.frame(y=y, X)
preds <- colnames(dat)[-1]

# Run forward selection with multiple steps
res <- build_paths(dat, "y", preds, delta=2, eps=0.5, L=5, K=4, verbose=TRUE, debug=FALSE)

# Inspect frontiers at each step
for(i in seq_along(res$path_forest)) {
  cat("Step", i-1, "frontier models:\n")
  print(res$path_forest[[i]])
  cat("\n")
}

# Access metadata
res$meta
}
